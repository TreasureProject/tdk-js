# Treasure.js React Examples

Install dependencies:

```bash
npm install
```

Create `.env` file and provide required environment variables:

```bash
cp .env.example .env
```

Start dev server:

```bash
npm run dev
```
