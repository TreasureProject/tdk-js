// Replace your-framework with the name of your framework

import { Button } from './Button';

const meta = {
    component: Button,
};

export const Example = {
    args: {
        primary: true,
        label: 'Button',
    },
};
export default meta;
